'use strict';

const name = 'ABCD EEEABFGH';

console.log();
console.log(`name = ${name}`);

console.log();
console.log(`name.indexOf('AB') = ${name.indexOf('AB')}`);
console.log(`name.lastIndexOf('A') = ${name.lastIndexOf('A')}`);
console.log(`name.includes(' EEE') = ${name.includes(' EEE')}`);
console.log(`name.startsWith('EEE') = ${name.startsWith('EEE')}`);
console.log(`name.endsWith('GH') = ${name.endsWith('GH')}`);